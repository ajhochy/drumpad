{
  "name": "drumrot",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Static SP-808 KILLA drum trainer. No bundle, no build. The Node deps below exist only for development helpers under scripts/.",
  "scripts": {
    "midi:pulse": "node scripts/midi-pulse.mjs",
    "midi:list": "node scripts/midi-list.mjs",
    "serve": "node .claude/serve.js"
  },
  "devDependencies": {
    "midi": "^2.0.0"
  }
}

# Accessibility pass — VoiceOver labels + reading order

> Plan ID #51 · GitHub #43 · Phase 9

## Goal
Every interactive element + every card labeled; pads have hints; reading order makes sense; drops grid uses combined elements per cell

## Likely files
- (all feature views)

## Acceptance criteria / tests
- [ ] Manual: real VoiceOver navigation across every tab; Accessibility Inspector clean

## Dependencies
Depends on: #28, #33, #35, #36, #37, #42

## Notes
- Manual merge only; branch per workflow run.
- Data safety: do not commit `.DS_Store`, Xcode user state, derived data, signing files, or generated images outside the asset catalog.

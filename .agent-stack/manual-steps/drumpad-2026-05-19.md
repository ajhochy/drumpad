# Manual Steps — drumpad 2026-05-19

## Issues Requiring Manual Verification

### #75
- Launch the Play tab and start a lesson — confirm horizontal grid lines scroll downward in sync with notes
- Verify four distinct brightness levels are visible: brightest at bar downbeats, progressively dimmer at quarter/eighth/sixteenth positions
- Verify grid lines do not obstruct note pads or the red strike line (correct z-ordering)
- Test at different BPMs (e.g., 72, 90, 100) to confirm grid stays in sync when tempo changes mid-play
- Test in Loop mode to confirm seamless grid crossfade at the loop boundary (no flicker or jump)
- Verify grid is hidden when the engine is in .idle phase (before hitting Play)


### #74
- Verify the 1/8 and 1/16 subdivision clicks sound correct at various tempos on a physical device
- Confirm the SUBDIV picker appears and disappears correctly when the Click toggle is toggled
- Check that the accent on beat 1 of each bar is clearly distinguishable from subdivision clicks in all three modes


### #73
- Verify the REC button appears in the Builder transport row and opens the record sheet
- Verify 4-beat count-in plays audible metronome clicks (accent on beat 1)
- Verify the step-progress bar moves in sync with the metronome at various BPM values
- Verify tapping pads during recording plays the drum sound and records hits into the correct step
- Verify completed loop hits are merged into the Builder grid without overwriting existing manual steps
- Verify starting a new loop clears the in-progress recording buffer for fresh capture
- Verify STOP ends recording cleanly and the sheet can be dismissed without crash
- Verify the record engine stops when BuildView disappears (tab switch)


### #72
- Verify achievement badge tiles render correctly in ProgressTabView (difficulty badge placement in topTrailing overlay)
- Verify streak-at-risk callout appears and disappears correctly based on today's practice state
- Verify OG explanation card renders correctly in DropsView info column
- Verify drumrot cards in DropsView show correct tier colors after 5-tier collapse (legendary should be amber, og should be white/prismatic)
- Verify creator achievement fires after 3 saves (not 1) in BuildView
- Verify secret achievements (slow_burn, coach) show padlock icon with no name or hint
- Verify existing saved collections survive the mythic->legendary and god->og migration without data loss on a device with existing progress
- Verify category headers (On the Kit, Showing Up, Speed Runs, Craft Crew) render in the achievement grid


### #71
- Tap ADD TO LIBRARY with a groove name already in the library — verify the 'Name Already Taken' alert appears with Replace, Save as New, and Cancel options
- Verify 'Replace' overwrites the existing groove and shows toast 'Replaced … in library'
- Verify 'Save as New' saves as 'Name (2)' (or next unused suffix) and shows toast 'Saved … to library'
- Verify 'Cancel' dismisses the alert without saving
- Verify auto-named grooves (empty name field) never trigger the overwrite alert


### #70
- Run xcodebuild test -scheme DrumrotUITests -destination 'platform=iOS Simulator,name=iPhone 16' to verify the 4 LibraryContextMenuUITests pass on a simulator
- Verify the --demo seeding of 'Backbeat Sketch' and 'demo-import' works correctly at app launch
- Confirm built-in lesson 'Rock Beat 101' exists in LessonCatalog.all so testBuiltInCellHasNoContextMenu does not XCTSkip


### #69
- Long-press a user-authored groove in the Library and confirm Rename appears in the context menu alongside Edit Groove and Delete
- Tap Rename, enter a new name, confirm the groove is renamed in the list
- Verify that entering an existing name shows an error and does not rename
- Verify built-in grooves do not show the Rename option in their context menu


### #68
- Verify in the web app that spamming a drum pad during a lesson lowers accuracy below what you'd get from playing nothing (ghost hits counted in denominator)
- Verify that hitting a pad with no note nearby breaks the combo in the web app
- Verify that the accuracy display updates correctly after a ghost hit
- iOS tests could not be run due to pre-existing Swift build failures in AppStore.swift and AchievementEngine.swift (unrelated to this change)


### #56
- Verify that the 3ms IO buffer preference is honored on target device hardware (A12+ silicon) — the diagnostic log in AudioSessionManager will show the actual granted buffer duration
- Confirm pre-warm eliminates first-hit latency spike by tapping a pad immediately after opening Play screen
- Test latency offset slider in Settings Audio pane adjusts judgment window perceptibly on a device with Bluetooth speaker


### #52
- Run scripts/testflight-upload.sh (with Apple account credentials) to archive and upload build 201 to TestFlight
- In App Store Connect → TestFlight → External Testing, create an 'External Beta' group, add the build, and submit for Beta App Review (~24 h)
- Enable the public link in the external group settings and distribute it to ≥3 testers
- Collect tester sessions and fill in docs/testing/smoke-notes.md as each tester completes their checklist
- Verify 0 P0/P1 bugs across all tester reports before marking issue #52 done


### #51
- Run scripts/capture-screenshots.sh on a Mac with Xcode + all three iPad simulator runtimes installed to generate the actual screenshot PNGs
- Run scripts/record-preview.sh with a booted simulator to record the 30-second preview .m4v
- Log into appstoreconnect.apple.com and paste the metadata from docs/app-store/asc-metadata.md into the drumrot record (description, keywords, What's New, support URL, privacy URL)
- Upload the generated screenshots into the iPad Pro 13", 12.9" 6th gen, and 11" device slots in ASC (4-6 per slot)
- Upload the .m4v preview video into the App Preview slot for at least one device class
- Toggle 'Available on Apple silicon Mac' ON in Pricing & Availability before submitting
- Fill in the Privacy Nutrition Label with 'Data Not Collected' (all No answers)
- Click Save → Add for Review to reach 'Ready for Review' state


### #48
- Run xcodebuild test -project ios/Drumrot.xcodeproj -scheme Drumrot -destination 'platform=iOS Simulator,name=iPad (10th generation)' to execute the UI suite on a booted simulator
- Verify testTabNavigation passes — the SPLidBar tab buttons use accessibilityLabel(tab.title) which maps to Play/Library/Progress/Build/Drops (note: Build not Builder — SmokeUITests checks for 'build' with CONTAINS predicate which should match)
- Verify testSettingsRoundTrip finds the gear button via 'gearshape.fill' accessibility label 'Settings' and that switches are present in the Feel pane (Haptics, Reduce Motion)
- Verify testBuilderLoadsLessonIntoPlay can find step cells via accessibilityLabel 'Step N, padname: on/off' pattern and the 'Load into Play' button text


### #47
- Verify the committed baseline PNG snapshots look visually correct for each view (DropsView, ProgressTabView, LibraryView, BuildView, ProgressTabView_NoUnlocks) — they are in ios/DrumrotSnapshotTests/__Snapshots__/DrumrotSnapshotTests/
- Confirm CI workflow 'Snapshot tests' step runs correctly on the GitHub Actions runner (macos-15) — the simulator UDID resolution logic may differ from local
- If baseline PNGs need re-recording (e.g. after a UI change), run: RECORD_SNAPSHOTS=1 xcodebuild -project ios/Drumrot.xcodeproj -scheme Drumrot -sdk iphonesimulator -destination 'id=<sim-udid>' -only-testing:DrumrotSnapshotTests test, then commit the updated PNGs


## Skipped Issues
None

## Build & TestFlight
- Review signing configuration in Xcode before submitting
- Verify bundle IDs match App Store Connect entries
- Confirm provisioning profiles are valid

## Build Results
Schemes found: Drumrot
- Drumrot: SUCCESS (archive at /tmp/drumpad-Drumrot.xcarchive)



## Additional Manual Steps
- To distribute the app (App Store or Ad Hoc), open the .xcarchive in Xcode Organizer and use the Distribute App workflow, which requires a valid Apple Developer signing identity and provisioning profile.
- The archive at /tmp/drumpad-Drumrot.xcarchive was built without code signing — it cannot be installed on a device or submitted to the App Store without re-signing.
